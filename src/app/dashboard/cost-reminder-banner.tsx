"use client";

// This component is no longer needed with the new trip system.
// Trips are now explicitly created by the owner (TripCost records),
// so there's no concept of "missing cost entries" anymore.
export default function CostReminderBanner() {
  return null;
}
